@model CartaListViewModel


<div class="container">
    <div class="row align-items-end">
        <div class="col-12">
            <div class="">
                <h1>
                    @Model.CategoriaAtual
                </h1>
            </div>
        </div>
            @foreach (var card in Model.Cartas)
            {
                <partial name="_CardsList" for="@card"/>
            }
    </div>
</div>